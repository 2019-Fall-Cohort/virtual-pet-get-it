const OrganicPet = require('./OrganicPet');

describe("OrganicPet", () => {
    // Assert
    const underTest = new OrganicPet();
    describe("renamePet", () => {
        test('should change name property to "Jessika"', () => {
            
        });
    })
})