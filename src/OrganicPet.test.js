const OrganicPet = require('./OrganicPet');

describe("OrganicPet", () => {
    // Arrange
    const underTest = new OrganicPet(undefined, id = 1);
    describe("renamePet", () => {
    
        });
    });
});